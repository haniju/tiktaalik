/**
 * Configuration de l'outil aérographe.
 * Paramètres destinés à être exposés dans l'interface ultérieurement.
 */
export const AIRBRUSH_CONFIG = {
  centerOpacity: 0.7,
  edgeOpacity: 0,
  radiusMultiplier: 8,
  pointDensity: 0.5,
  maxAccumulatedOpacity: 0.95,
};
